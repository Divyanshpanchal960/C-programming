#include <stdio.h>
    int main()
{
       char ch;
       
       printf("enter the character");
       scanf("%c", &ch);
       
       if(ch=='a')
       {
           int a=12,b=45,result=a+b;
           printf("result=%d\n",result);
           
           printf("addition of two number");
           }
       else{
           printf("input is not matched");
           
       }
}